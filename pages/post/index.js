import FeaturedPost from "../../components/FeaturedPost";
import Grid from "@material-ui/core/Grid";
// eslint-disable-next-line import/no-anonymous-default-export
export default function post() {
  return <></>;
  //     return (<Grid container spacing={4}>
  //     <Grid item xs={12} md={9}>
  //         <h2 style={{ textAlign: 'Center', paddingTop:'50px' }}>熱門文章</h2>
  //         {getPostByHot()?.map((post) => (<FeaturedPost key={post.title} post={post} />))}
  //         <h2 style={{ textAlign: 'Center', paddingTop:'50px' }}>最新文章</h2>
  //         {getPostByNew()?.map((post) => (<FeaturedPost key={post.title} post={post} />))}
  //     </Grid>
  // </Grid>);
}
